import React from "react";

import Typography from "@mui/material/Typography";

const FormComplete: React.VFC = () => {
  return (
    <div>
      <Typography>フォームの送信に成功しました</Typography>
    </div>
  );
}

export default FormComplete;
